import { IdenticalPasswordsDirective } from './identical-passwords.directive';

describe('IdenticalPasswordsDirective', () => {
  it('should create an instance', () => {
    const directive = new IdenticalPasswordsDirective();
    expect(directive).toBeTruthy();
  });
});
